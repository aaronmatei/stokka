import {
  ADD_USER,
  DELETE_USER,
  SET_CURRENT_USER,
  CLEAR_CURRENT_USER,
  UPDATE_USER,
  FILTER_USERS,
  CLEAR_FILTER,
  SET_ALERT,
  REMOVE_ALERT
} from '../types';

const userReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default userReducer;

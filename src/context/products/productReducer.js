import {
  ADD_PRODUCT,
  DELETE_PRODUCT,
  UPDATE_PRODUCT,
  FILTER_PRODUCTS,
  SET_ALERT,
  REMOVE_ALERT
} from '../types';

const productReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default productReducer;
